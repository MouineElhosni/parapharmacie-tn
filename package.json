{
  "name": "parapharmacie-tn",
  "version": "1.0.0",
  "description": "Parapharmacie.Tn - boutique en ligne (Node.js + React + MySQL)",
  "scripts": {
    "build": "npm ci --prefix frontend && npm run build --prefix frontend && npm ci --omit=dev --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
